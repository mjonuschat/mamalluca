[package]
name = "moonraker-types"
description = "Type definitions for Moonraker 3D printer management server"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
